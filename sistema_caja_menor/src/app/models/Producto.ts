export class Producto {
    id: string;
    nombre: string;
    precio: string;

    constructor(id: string, nombre: string, precio: string) {
        this.id = id
        this.nombre = nombre
        this.precio = precio
    }
}